{
  "id": "/push",
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "$push",
  "description": "Appends a value to a data series",
  "type": "object",
  "additionalProperties": false,
  "properties": {
  	"$series": {
  		"type": "string",
  		"description": "The name of the series to which the value is to be appended"
  	},
    "$value": {
      "type": "number",
      "description": "The value to be appended"
    },
    "$when": {
      "type": "integer",
      "description": "The time at which the data point should be recorded"
    }
  },
  "required": [
    "$series", "$value"
  ]
}